import { EnviorementPipe } from './enviorement.pipe';

describe('EnviorementPipe', () => {
  it('create an instance', () => {
    const pipe = new EnviorementPipe();
    expect(pipe).toBeTruthy();
  });
});
