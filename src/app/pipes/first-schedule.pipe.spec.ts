import { FirstSchedulePipe } from './first-schedule.pipe';

describe('FirstSchedulePipe', () => {
  it('create an instance', () => {
    const pipe = new FirstSchedulePipe();
    expect(pipe).toBeTruthy();
  });
});
