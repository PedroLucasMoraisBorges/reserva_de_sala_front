import { BuildingNamePipe } from './building-name.pipe';

describe('BuildingNamePipe', () => {
  it('create an instance', () => {
    const pipe = new BuildingNamePipe();
    expect(pipe).toBeTruthy();
  });
});
